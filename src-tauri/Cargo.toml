[package]
name = "galeon"
version = "1.0.0-alpha.3"
description = "Galeon Object Storage Client"
authors = ["Hanafee Hama"]
license = "MIT"
repository = "https://github.com/fiztoz/galeon"
edition = "2021"
rust-version = "1.94.1"

[lib]
name = "galeon_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
opendal = { version = "0.59", features = ["services-s3", "services-sftp", "executors-tokio", "layers-throttle"] }
ssh2 = { version = "0.9", features = ["vendored-openssl"] }
suppaftp = { version = "^11.0.0", features = ["native-tls"] }
uuid = { version = "1.8", features = ["v4", "serde"] }
futures-util = "0.3"
chrono = { version = "0.4", features = ["serde"] }
glob = "0.3"
keyring = { version = "3", features = ["apple-native", "sync-secret-service"] }
lazy_static = "1.5.0"
md5 = "0.7"
dirs = "5.0"
notify = "8.2.0"
aws-config = "1.12.0"
aws-credential-types = "1"
# 1.145+ uses patched lru; `rustls` would re-enable the legacy Hyper 0.14 stack.
aws-sdk-s3 = { version = "1.145.0", default-features = false, features = ["sigv4a", "http-1x", "default-https-client", "rt-tokio"] }
# Public Smithy HTTP traits used by the explicit metadata TLS-bypass adapter.
aws-smithy-runtime-api = { version = "1", features = ["client", "http-1x"] }
aws-smithy-types = { version = "1", features = ["http-body-1-x"] }
urlencoding = "2"
# Match OpenDAL's reqwest major for HttpClient::with(reqwest::Client)
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "http2", "stream", "charset"] }

[dev-dependencies]
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
rcgen = "0.13"
tauri = { version = "2", features = ["test"] }
tokio-rustls = { version = "0.26", default-features = false, features = ["ring", "tls12"] }
